# 猫咪小院 · 像素流浪猫物语

像素风猫咪布置与互动小游戏。选择猫粮碗、毛线球、清水碗、睡垫和抓抓柱，在小院里摆放来吸引流浪猫；点击猫咪摸摸它，也可以与院内物品互动。游戏进度保存在本地浏览器。

## 本地游玩

直接打开 `index.html` 即可。主页依赖同目录下的 `style.css` 和 `game.js`。

## 发布到 GitHub Pages

将 `index.html`、`style.css`、`game.js` 和 `.nojekyll` 上传到仓库根目录，然后在 **Settings → Pages** 选择 `main` 分支和 `/(root)`。
